<footer class="footer">
    <div class="c-md">
        <h1 class="footer__title"><%= translations['footer.products'].toUpperCase() %></h1>
        <div class="footer__list">
            <% products.forEach(product => { %>
                    <a class="footer__item" href="/<%= locale %>/product/<%= product.name.toLowerCase() %>">
                        <%= product.name %>
                        <span class="footer__secondary">
                            - <%= product.description %>
                        </span>
                    </a>
            <% }) %>
        </div>
        <a href="/<%= locale %>" class="footer__brand">
            Ecolier
            <span class="footer__brand-spirit">
                <%= translations['footer.spirit'] %>
            </span>
        </a>
    </div>
</footer>